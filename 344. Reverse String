class Solution {
public:
    string reverseString(string s) {
        int i,t,l=s.length();
        for(i=l-1;i>=l/2;i--){
            t=s[i];
            s[i]=s[l-i-1];
            s[l-i-1]=t;
        }
    return s;
    }
};
